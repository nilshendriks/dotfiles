- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.zshrc:
    ~/.gitconfig:

- create:
    - ~/Work

- shell:
    - 
      command: ./setup_homebrew.sh
      stdout: true
      stderr: true
